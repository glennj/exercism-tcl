!* variableSubst true
!* commandSubst true
$header

! set i 0
! foreach case $cases {
!   set test_id "${slug}-[incr i]"
!   if {$i > 1} {
skip $test_id
!   }
test $test_id [testTitle $case] -body {
    sumOfMultiples [asList [dict get $case input factors]] [dict get $case input limit]
} -returnCodes ok -result [dict get $case expected]

! }

$footer
